plugins {
    id 'java'
    id 'application'
    id 'com.google.protobuf' version '0.8.19'
}

repositories {
    mavenCentral()
    mavenLocal()
    maven {
        url "${artifactory_contextUrl}/gradle"
        credentials {
            username = "${artifactory_user}"
            password = "${artifactory_password}"
        }
    }
}

dependencies {
    implementation 'io.grpc:grpc-netty-shaded:1.54.0'
    implementation 'org.hypertrace.core.documentstore:document-store:0.7.61'
    implementation 'io.grpc:grpc-protobuf:1.54.0'
    implementation 'io.grpc:grpc-stub:1.54.0'
    implementation 'com.fasterxml.jackson.core:jackson-databind:2.13.3'
    implementation 'com.google.protobuf:protobuf-java-util:4.27.2'
    implementation 'javax.annotation:javax.annotation-api:1.3.2'
    testImplementation 'junit:junit:4.13.2'
}

protobuf {
    protoc {
        artifact = 'com.google.protobuf:protoc:3.21.12'
    }
    plugins {
        grpc {
            artifact = 'io.grpc:protoc-gen-grpc-java:1.54.0'
        }
    }
    generateProtoTasks {
        all().each { task ->
            task.plugins {
                grpc {}
            }
        }
    }
}

sourceSets {
    main {
        java {
            srcDirs 'src/main/java', 'build/generated/source/proto/main/java', 'build/generated/source/proto/main/grpc'
        }
    }
}

application {
    mainClassName = 'bookstore.BookServer'
}
